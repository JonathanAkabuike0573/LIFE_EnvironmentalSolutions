<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LIFE_EnvironmentalSolution" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LIFE_EnvironmentalSolution" parent="Base.Theme.LIFE_EnvironmentalSolution" />

    <style name="SubMenuTextStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:textColor">@color/red</item>
    </style>
    <style name="Theme.SplashWorks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- SplashScreen background for light mode -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Animated icon -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.SplashWorks</item>
    </style>
</resources>